/build/*
!/build/.npmkeep

# Keystore files - never commit
*.keystore
*.jks
keystore-pass.txt
# Release signing secrets - never commit
RELEASE_SECRETS.txt
keystore-b64.txt
# Firebase config - written from CI secret, never commit
google-services.json
