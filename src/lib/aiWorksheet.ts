// AI สร้างใบงาน — พิมพ์ “สร้างใบงาน ป.5 คณิต เรื่องเศษส่วน” → AI สร้าง PDF พร้อมเฉลย
export async function generateWorksheet(prompt: string){ return { html: `<h1>${prompt}</h1><p>ใบงาน AI ตัวอย่าง</p>`, answer:"เฉลย" } }
